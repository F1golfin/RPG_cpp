#ifndef ENUMS_H
#define ENUMS_H

enum class MonsterType { NORMAL, MINIBOSS, BOSS };
enum class ItemType { HEAL };
enum class EndType { GENOCIDE, PACIFIST, NEUTRAL, DEFEAT };
enum class EncounterStatus { NOT_FOUGHT, KILLED, SPARED };

#endif
